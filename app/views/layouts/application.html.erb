<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= page_title %></title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <!-- Just for debugging purposes. Don't actually copy this line! -->
  <!--[if lt IE 9]><script src="../../assets/js/ie8-responsive-file-warning.js"></script><![endif]-->

  <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
  <![endif]-->
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= yield :head %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= body_class %>">

<%= render "shared/navbar" %>

<div class="container-fluid">
  <div class="row" style="padding-top: 60px">
    <div class="col-sm-3 col-md-2 sidebar">
      <ul class="nav nav-sidebar">
        <li<%if @nav_item == "dashboard" %> class="active"<% end %>><%= link_to "Dashboard", root_path %></li>
<% if user_signed_in? && current_user.super_admin? %>
        <li<%if @nav_item == "dashboard" %> class="active"<% end %>><%= link_to "Aministration", admin_root_path %></li>
<% end %>
      <ul>
    </div>
    <div class="col-sm-9 col-md-10 main">

      <h1 class="page-header"><%= yield :page_heading %><% if content_for? :page_sub_heading %>  <small><%= yield :page_sub_heading %></small><% end %> </h1>

<%= render "shared/flash" %>

<%= yield %>
    </div>
  </div>
</div>

<%= yield :footer %>
</body>
</html>
